header.header 
  nav.nav.nav--tours
    //- he slush replaced the # ie it take you back to the overview page
    a.nav__el(href='/') All tours 
  .header__logo 
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
          //-  user in isLoggedIn in the authController
    if user
        //- Dispalys the logged in page(a page where by one is logged in)
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me') 
        img.nav__user-img(src= `/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span user.name.split(' ')[0]
            //- else if the user is not logged in goes to either Log in or sign up
    else
    a.nav__el(href='/login') Log in
    a.nav__el.nav__el--cta(href='#') Sign up